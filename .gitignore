# Quarto build artifacts
/_site/
/.quarto/

# Keep /_freeze/ committed: CI and re-renders reuse cached code output,
# so deploys are fast and reproducible. Do not ignore it.

# Python
__pycache__/
*.pyc
.venv/
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

**/*.quarto_ipynb
**/*.quarto_ipynb_*
posts/**/index_files/

# Design-system export artifacts (scratch from the design tooling).
# The published site only consumes the token CSS in tokens/ and styles.css;
# these exports are kept locally for reference but must not enter the repo.
/components/
/ui_kits/
/guidelines/
/publish/
/scraps/
/uploads/
/_ds_bundle.js
/_ds_manifest.json
/Math Notes - Website.html
/SKILL.md
/_adherence.oxlintrc.json
.gstack/
